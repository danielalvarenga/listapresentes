<%= simple_form_for(@mensagem) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :remetente %>
    <%= f.input :texto %>
    <%= f.association :presente %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
